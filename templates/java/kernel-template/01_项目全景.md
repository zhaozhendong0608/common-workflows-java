# 01_项目全景 (Landscape)

> **说明**: 描述项目的技术栈、模块划分、依赖关系

## 项目概述

**项目名称**: {{project.name}}

**业务领域**: {{project.description}}

**核心功能**:
{% for module in modules -%}
- {{module.name}} - {{module.description}}
{% endfor -%}

---

## 技术选型

| 类别 | 技术选型 | 版本 | 选型理由 |
|------|---------|------|---------|
| Web框架 | {{tech.backend.framework}} | {{tech.backend.version}} | 成熟稳定，生态丰富 |
| 持久层 | {{tech.orm.name}} | {{tech.orm.version}} | 简化 CRUD，支持代码生成 |
| 数据库 | {{tech.database.type}} | {{tech.database.version}} | 关系型数据，事务支持 |
| 缓存 | {{tech.cache.type}} | {{tech.cache.version}} | 高性能，支持多种数据结构 |
{% if tech.frontend.framework != "None" -%}
| 前端框架 | {{tech.frontend.framework}} | {{tech.frontend.version}} | 响应式，组件化 |
| UI库 | {{tech.frontend.ui_library}} | - | 组件丰富，符合中后台场景 |
| 构建工具 | {{tech.frontend.build_tool}} | - | 快速热更新，原生 ESM |
{% endif -%}
{% if tech.microservices.enabled -%}
| 注册中心 | {{tech.microservices.registry}} | - | 服务发现与配置管理 |
| API网关 | {{tech.microservices.gateway}} | - | 统一入口，路由转发 |
{% if tech.microservices.mq != "None" -%}
| 消息队列 | {{tech.microservices.mq}} | - | 异步解耦，削峰填谷 |
{% endif -%}
{% endif -%}

---

## 模块划分

### 后端模块

```
com.company.{{project.code}}/
├── api/ # 对外 API
│ ├── controller/ # 控制器
│ └── dto/ # 数据传输对象
│
├── service/ # 业务逻辑
│ ├── impl/ # 实现类
{% for module in modules -%}
│ ├── {{module.code}}/
│ │   └── {{module.code | capitalize}}Service.java
{% endfor -%}
│
├── repository/ # 数据访问
│ ├── mapper/ # MyBatis Mapper
│ └── entity/ # 实体类
│
├── common/ # 通用组件
│ ├── config/ # 配置类
│ ├── exception/ # 异常处理
│ └── util/ # 工具类
│
{% for module in modules -%}
└── {{module.code}}/ # {{module.name}}模块
    ├── controller/
    ├── service/
    └── mapper/
{% endfor -%}
```

{% if tech.frontend.framework != "None" -%}
### 前端模块

```
src/
├── views/ # 页面组件
{% for module in modules -%}
│ ├── {{module.code}}/ # {{module.name}}模块
{% endfor -%}
│ └── layout/ # 布局
│
├── components/ # 通用组件
│ ├── common/ # 公共组件
│ └── business/ # 业务组件
│
├── api/ # API 接口
{% for module in modules -%}
│ ├── {{module.code}}.js
{% endfor -%}
│
├── router/ # 路由配置
├── store/ # 状态管理
└── utils/ # 工具函数
```
{% endif -%}

---

## 核心流程

### 请求处理流程

```
{% if tech.microservices.enabled -%}
客户端请求
    ↓
API 网关 ({{tech.microservices.gateway}})
    ↓ (路由转发)
微服务 Controller
    ↓ (参数验证)
Service 层
    ↓ (业务逻辑)
Mapper 层
    ↓ (SQL 执行)
{{tech.database.type}} 数据库
    ↓ (返回结果)
响应客户端
{% else -%}
客户端请求
    ↓
Controller 层
    ↓ (参数验证)
Service 层
    ↓ (业务逻辑)
Mapper 层
    ↓ (SQL 执行)
{{tech.database.type}} 数据库
    ↓ (返回结果)
响应客户端
{% endif -%}
```

### 缓存策略

```
查询请求
    ↓
检查 {{tech.cache.type}} 缓存
    ↓ (未命中)
查询 {{tech.database.type}}
    ↓
写入 {{tech.cache.type}}
    ↓
返回结果
```

---

## 数据模型

### 核心实体

{% for module in modules -%}
#### {{module.name}}模块

**实体表**: `t_{{module.code}}`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| ... | ... | ... |

{% endfor -%}

---

## 接口规范

### RESTful API

**基础路径**: `/api/v1`

**模块路由**:
{% for module in modules -%}
- `{{module.code}}` - {{module.name}}
{% endfor -%}

**统一响应格式**:

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

---

## 性能指标

### 关键指标

| 指标 | 目标值 | 监控方式 |
|------|--------|---------|
| API 响应时间 | < 200ms | Prometheus |
| 数据库查询 | < 50ms | Slow Query Log |
| 缓存命中率 | > 80% | {{tech.cache.type}} Monitor |
| 并发用户数 | 500+ | JMeter 压测 |

---

## 部署架构

{% if tech.microservices.enabled -%}
### 微服务部署

```
Nginx (负载均衡)
    ↓
{{tech.microservices.gateway}} (网关集群)
    ↓
{{tech.microservices.registry}} (服务注册)
    ↓
{% for module in modules -%}
{{module.name}}服务 (3+ 实例)
{% endfor -%}
    ↓
{{tech.database.type}} 主从集群
{{tech.cache.type}} 哨兵集群
```
{% else -%}
### 单体部署

```
Nginx (负载均衡)
    ↓
应用服务器 (3+ 实例)
    ↓
{{tech.database.type}} 主从
{{tech.cache.type}} 集群
```
{% endif -%}

---

## 监控告警

### 监控指标
- **应用监控**: Spring Boot Actuator
- **日志采集**: ELK Stack
- **链路追踪**: SkyWalking / Zipkin
- **性能分析**: Arthas

### 告警策略
- **CPU 使用率**: > 80% 告警
- **内存使用率**: > 85% 告警
- **API 错误率**: > 1% 告警
- **响应时间**: > 500ms 告警

---

## 技术债务

### 已知问题
- [ ] 待补充

### 优化计划
- [ ] 待补充

---

**创建时间**: {{_timestamp}}
**最后更新**: {{_timestamp}}
