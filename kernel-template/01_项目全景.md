# 01_项目全景 (Landscape)

> **说明**: 描述项目的技术栈、模块划分、依赖关系

## 项目概述

**项目名称**: {PROJECT_NAME}

**业务领域**: {BUSINESS_DOMAIN}

**核心功能**:
- 功能模块 1
- 功能模块 2
- 功能模块 3

---

## 技术选型

| 类别 | 技术选型 | 版本 | 选型理由 |
|------|---------|------|---------|
| Web框架 | Spring Boot | {VERSION} | 成熟稳定，生态丰富 |
| 持久层 | MyBatis Plus | {VERSION} | 简化 CRUD，支持代码生成 |
| 数据库 | MySQL | {VERSION} | 关系型数据，事务支持 |
| 缓存 | Redis | {VERSION} | 高性能，支持多种数据结构 |
| 前端框架 | Vue 3 | {VERSION} | 响应式，组件化 |
| UI库 | Element Plus | {VERSION} | 组件丰富，符合中后台场景 |

---

## 模块划分

### 后端模块

```
com.company.project/
├── api/ # 对外 API
│ ├── controller/ # 控制器
│ └── dto/ # 数据传输对象
│
├── service/ # 业务逻辑
│ ├── impl/ # 实现类
│ └── {MODULE}Service.java
│
├── repository/ # 数据访问
│ ├── mapper/ # MyBatis Mapper
│ └── entity/ # 实体类
│
├── common/ # 通用组件
│ ├── config/ # 配置类
│ ├── exception/ # 异常处理
│ └── util/ # 工具类
│
└── {MODULE}/ # 业务模块（可选）
 ├── controller/
 ├── service/
 └── repository/
```

### 前端模块

```
src/
├── views/ # 页面组件
│ ├── {module}/ # 业务模块
│ └── layout/ # 布局
│
├── components/ # 通用组件
│ ├── common/ # 公共组件
│ └── business/ # 业务组件
│
├── api/ # API 接口
│ └── {module}.js
│
├── router/ # 路由配置
├── store/ # 状态管理
└── utils/ # 工具函数
```

---

## 分层架构

```
┌─────────────────────────────────┐
│ 前端层 (Vue) │
│ ┌──────────┐ ┌──────────┐ │
│ │ 页面组件 │ │ 通用组件 │ │
│ └──────────┘ └──────────┘ │
└─────────────────────────────────┘
 ↕ HTTP/JSON
┌─────────────────────────────────┐
│ 后端层 (Spring Boot) │
│ ┌──────────────────────────┐ │
│ │ Controller (接口层) │ │
│ └──────────────────────────┘ │
│ ↕ │
│ ┌──────────────────────────┐ │
│ │ Service (业务逻辑层) │ │
│ └──────────────────────────┘ │
│ ↕ │
│ ┌──────────────────────────┐ │
│ │ Repository (数据访问层) │ │
│ └──────────────────────────┘ │
└─────────────────────────────────┘
 ↕ JDBC
┌─────────────────────────────────┐
│ 数据层 (MySQL) │
│ ┌──────────┐ ┌──────────┐ │
│ │ 业务数据 │ │ 系统数据 │ │
│ └──────────┘ └──────────┘ │
└─────────────────────────────────┘
```

---

## 核心模块说明

### 模块 1: {MODULE_NAME}

**职责**: {RESPONSIBILITY}

**核心类**:
- `{CLASS_1}Controller`: API 接口
- `{CLASS_1}Service`: 业务逻辑
- `{CLASS_1}Mapper`: 数据访问
- `{CLASS_1}Entity`: 数据实体

**数据库表**:
- `{table_name}`: {TABLE_DESCRIPTION}

---

### 模块 2: {MODULE_NAME}

（根据实际项目填充）

---

## 依赖关系

### 模块依赖

```
用户模块 ←── 权限模块
 ↓
部门模块 ←── 组织架构模块
```

### 第三方依赖

| 依赖 | 用途 | 版本 |
|------|------|------|
| lombok | 减少样板代码 | {VERSION} |
| hutool | 工具类库 | {VERSION} |
| fastjson | JSON 处理 | {VERSION} |
| knife4j | API 文档 | {VERSION} |

---

## 环境配置

### 开发环境
- **JDK**: {JDK_VERSION}
- **Node.js**: {NODE_VERSION}
- **Maven/Gradle**: {BUILD_TOOL_VERSION}
- **IDE**: IntelliJ IDEA / VS Code

### 数据库环境
- **开发库**: {DEV_DB}
- **测试库**: {TEST_DB}
- **生产库**: {PROD_DB}

### 外部服务
- **缓存**: Redis {HOST}:{PORT}
- **消息队列**: {MQ_HOST}:{MQ_PORT} (可选)

---

## 部署架构

```
┌─────────────────────┐
│ Nginx (负载均衡) │
└─────────────────────┘
 ↓
┌─────────────────────┐
│ Spring Boot 实例 │ × N
└─────────────────────┘
 ↓
┌─────────────────────┐
│ MySQL (主从) │
└─────────────────────┘
 ↓
┌─────────────────────┐
│ Redis (集群) │
└─────────────────────┘
```

---

## 接口规范

### RESTful API 规范

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/{resource}` | 查询列表 |
| GET | `/api/{resource}/{id}` | 查询详情 |
| POST | `/api/{resource}` | 创建 |
| PUT | `/api/{resource}/{id}` | 更新 |
| DELETE | `/api/{resource}/{id}` | 删除 |

### 响应格式

```json
{
 "code": 200,
 "message": "success",
 "data": {...}
}
```

---

## 性能指标

| 指标 | 目标值 |
|------|--------|
| 接口响应时间 (P95) | <500ms |
| 并发用户数 | >{CONCURRENT_USERS} |
| 数据库连接池 | {POOL_SIZE} |
| 缓存命中率 | >80% |

---

**最后更新**: {UPDATE_DATE}
**维护人**: {MAINTAINER}

